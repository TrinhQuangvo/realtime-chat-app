import SignInTemplate from '@/screens/sign-in'
import React from 'react'

type Props = {}

const SignIn: React.FC<Props> = (props: Props) => {
    return (
        <SignInTemplate />
    )
}

export default SignIn 