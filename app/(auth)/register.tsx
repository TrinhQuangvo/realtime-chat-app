import RegisterTemplate from '@/screens/register'
import React from 'react'

type Props = {}

const Register: React.FC<Props> = (props: Props) => {
    return (
        <RegisterTemplate />
    )
}

export default Register 